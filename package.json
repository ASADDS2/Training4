{
  "name": "vetcare-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "npx json-server --watch public/database.json --port 3000",
    "start": "npm run server & npm run dev"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
